Place the looping Projector Bach track at:

```text
public/audio/projector-bach.mp3
```

The site references it as `/audio/projector-bach.mp3` and will loop it on entry
when the browser allows autoplay.
